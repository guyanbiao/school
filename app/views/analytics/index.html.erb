你已经做了<%= current_user.records.count %>次测试
看起来你最不明白的几个知识点是<br/>
<% @ranking.each do |ranking| %>
<% @get = show_resourse(ranking.point) %>
<%= t("analytics.index."+ranking.point.type) %>
<% if ranking.point.type == "distinguish phrase" %>
  <% ranking.point.core.each do |p| %>
    <%= p %>
  <% end %>
<% else %>
  <%= t("analytics.index."+ranking.point.core) %>
<% end %>

  <%= t(@get.pre) %> <%= eval(@get.show) %> <%= eval(@get.first) %>  <%= eval(@get.second) %> <%= eval(@get.third) %><br/> 
<% end %>

