<% @show_resource = Proc.new do |point| 
  "#{t('analytics.index.'+point.type.to_s)} #{point.core}"+
case point.type 
when "word","phrase"
  " 的意思"
when "distinguish phrase"
  "kkkkkk"
when "grammar", "sentence pattern", "conjunction"
  " 的应用"
else 
  "lalaal"
end
 end %>

你已经做了<%= current_user.records.count %>次测试
看起来你最不明白的几个知识点是<br/>
<% @ranking.each do |ranking| %>
  <%= ranking.point.id %>  <%= @show_resource.call ranking.point%> <%= link_to "Show hitory", reviews_path(point_id: ranking.point.id) %>
  <%= link_to "Edit", edit_point_path(ranking.point.id) %> <%= link_to "Learn", point_path(ranking.point.id) %><br/>
<% end %>

