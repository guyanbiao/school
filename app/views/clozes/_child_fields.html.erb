<%= f.text_field :choices %>
<%= f.text_field :answer %><%= link_to 'New Point', build_points_path(:id => f.object.id) %>
<%= f.fields_for :points do |builder| %>
  <%= render 'point_fields', :f => builder %>
<% end %>

